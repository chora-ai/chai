[
  {
    "name": "rss_check_feed",
    "description": "Fetch an RSS or Atom feed and return a parsed table of entries (title, date, link, summary). Accepts a feed name (from configured feeds) or a direct URL.",
    "parameters": {
      "type": "object",
      "required": ["feed"],
      "properties": {
        "feed": {
          "type": "string",
          "description": "Feed name from configured feeds (e.g. 'hackernews') or a direct feed URL"
        }
      }
    }
  },
  {
    "name": "rss_list_feeds",
    "description": "List all configured feed names and their URLs.",
    "parameters": {
      "type": "object",
      "required": [],
      "properties": {}
    }
  }
]
